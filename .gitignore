# Dependencies
node_modules/
.venv/
venv/
__pycache__/
*.pyc
*.pyo
.env

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Build
dist/
build/
.svelte-kit/
.pio/

# Generated data/models
data/
edge-hub/models/
models/checkpoints/

# LLM tooling (never committed)
.claude/
CLAUDE.md
deepseek.md
REPORULES.md
summary.md
# NOTE: AGENTS.md, state.md, STATE.md are TRACKED (worker contract + repo state). Do not ignore them.
# (This repo uses uppercase STATE.md for tracked repo state; lowercase state.md is also tracked per worker contract.)

# Agent work files (per-session, not tracked — per AGENTS.md worker contract)
workplan.md
lessons.md

# Type checking
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Coverage
coverage/
htmlcov/
.coverage
coverage.xml

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# --- Security baseline (do not remove) ---
token.json
token_*.json
*token*.json
*.pickle
client_secret*.json
credentials.json
# ponytail: firmware secrets are user-filled per-node. Track only the .template;
# the real secrets.h is untracked here and gitignored so a careless add can't ship it.
firmware/src/secrets.h
*.db-shm
*.db-wal
*.sqlite
